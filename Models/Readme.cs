using System;

namespace Models
{
    public class Readme
    {
        /*
         * 1. objects are passed by ref
         * 2. use interfaces to generalize
         * 3. use extensions to extend your class capapilities
         * 4. each service is indepenent and its suppose to handle its validations
         * 5. ise flunet api to separate validation from models and extra capabilites
         */
    }
}